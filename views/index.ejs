<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= page.title %></title>
    <link rel="stylesheet" href="/css/Main.css" />
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="frase">
      <h1 class="h1">Automatizar. Criar.</h1>
      <h1>Farmar!</h1>
      <div class="cta">
        <button class="scroll-btn" onclick="scrollToSection()">
          <h2>Farm mais fundo</h2>
        </button>
        <div class="parallax">
          <img src="/img/parallax.png" alt="">
        </div>
      </div>

      <div class="imagem_especifica">
        <img src="/img/bagulhos 3.png" alt="">
      </div>
    </div>

    <section class="allpage">
      <section class="content-section">
        <h1>INFORMATIVO</h1>
        <div class="tabs">
          <button class="tab" data-tab="team" onclick="switchTab('team')">
            <h2>Equipe</h2>
          </button>
          <button class="tab" data-tab="site" onclick="switchTab('site')">
            <h2>Site</h2>
          </button>
        </div>

        <div id="team" class="tab-content">
          <div class="team-container">
            <% if (page.team && page.team.length > 0) { %>
              <% page.team.forEach(member => { %>
                <div class="team-member">
                  <img src="<%= member.imgSrc %>" alt="<%= member.imgAlt %>" />
                  <div>
                    <h2><%= member.name %></h2>
                    <p><%= member.description %></p>
                  </div>
                </div>
              <% }) %>
            <% } else { %>
              <p>Nenhum membro da equipe encontrado.</p>
            <% } %>
          </div>
        </div>

        <div id="site" class="tab-content">
          <div class="site-info">
            <p><%= page.siteInfo %></p>
          </div>
        </div>
      </section>
    </section>

    <!-- Slider -->
    <div class="slider" id="slideimg">
      <!-- List Items -->
      <div class="list">
        <% if (page.slider && page.slider.length > 0) { %>
          <% page.slider.forEach((slide, index) => { %>
            <div class="item <%= index === 0 ? 'active' : '' %>">
              <img src="<%= slide.imgSrc %>" alt="<%= slide.imgAlt %>">
              <div class="content">
                <p><%= slide.tag %></p>
                <h2><%= slide.title %></h2>
                <p><%= slide.description %></p>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p>Nenhum slide encontrado.</p>
        <% } %>
      </div>
      <!-- Button Arrows -->
      <div class="arrows">
        <button id="prev"><</button>
        <button id="next">></button>
      </div>
      <!-- Thumbnail -->
      <div class="thumbnail">
        <% if (page.slider && page.slider.length > 0) { %>
          <% page.slider.forEach((slide, index) => { %>
            <div class="item <%= index === 0 ? 'active' : '' %>">
              <img src="<%= slide.imgSrc %>" alt="<%= slide.imgAlt %>">
              <div class="content">
                <%= slide.title %>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p>Nenhum thumbnail encontrado.</p>
        <% } %>
      </div>
    </div>

    <article>
      <img src="/img/bagulhos 2.png" alt="">
      <h2>Minefarms</h2>
      <p>
        A engenharia de redstone e as farms são pilares essenciais no Minecraft, permitindo automação e otimização de recursos. 
        Com redstone, jogadores podem criar circuitos complexos e sistemas que simplificam tarefas, desde portas automáticas até mecanismos avançados. 
        As farms utilizam essa tecnologia para coletar itens de forma contínua, como alimentos, minerais e até drops de mobs, acelerando o progresso e tornando o jogo mais estratégico. 
        No MineFarms, exploramos e ensinamos as melhores técnicas de construção de farms, ajudando jogadores a maximizar eficiência e recursos para expandir seus mundos com criatividade e facilidade.
      </p>
      
      <img src="/img/Apresentações/Captura de tela 2024-11-24 120619.png" class="engimg" alt="">
    </article>

    <div class="dicas-section">
      <div class="dicas-main">
        <h2 class="section-title">Dicas</h2>
        <div class="dicas-cards">
          <% if (page.dicas && page.dicas.length > 0) { %>
            <% page.dicas.forEach(dica => { %>
              <div class="dica-card">
                <img src="<%= dica.imgSrc %>" alt="<%= dica.imgAlt %>">
                <div class="dica-content">
                  <h3><%= dica.title %></h3>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <p>Nenhuma dica encontrada.</p>
          <% } %>
        </div>
        <a href="#" class="ver-mais">Ver mais &rarr;</a>
      </div>
      <div class="dicas-side">
        <% if (page.sideDicas && page.sideDicas.length > 0) { %>
          <% page.sideDicas.forEach(sideDica => { %>
            <div class="side-dica">
              <img src="<%= sideDica.imgSrc %>" alt="<%= sideDica.imgAlt %>">
              <div class="side-content">
                <h4><%= sideDica.title %></h4>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p>Nenhuma dica lateral encontrada.</p>
        <% } %>
      </div>
    </div>

    <%- include('partials/footer') %>

    <!-- Scripts -->
    <script src="/js/Tabs.js"></script>
    <script src="/js/Slide.js"></script>
    <script src="/js/teste.js"></script>
    <script src="/public/js/botaomainpage.js"></script>
  </body>
</html>
